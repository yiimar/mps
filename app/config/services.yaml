# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\Infrastructure\:
        resource: '../src/Infrastructure'
        exclude:
            - '../src/Infrastructure/Symfony7/Kernel.php'

    # Controllers
    App\Ui\Http\Controller\:
        resource: '../src/Ui/Http/Controller/'
        tags: [ 'controller.service_arguments' ]

    # CommandBus and QueryBus
#    App\Infrastructure\CommandBus\TacticianCommandBus:
#        arguments:
#            $commandBus: '@tactician.commandbus.write'
#    App\Infrastructure\QueryBus\TacticianQueryBus:
#        arguments:
#            $commandBus: '@tactician.querybus.read'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
#    App\Infrastructure\CommandBus\CommandBus: 'App\Infrastructure\CommandBus\TacticianCommandBus'
#    App\Infrastructure\QueryBus\QueryBus: 'App\Infrastructure\QueryBus\TacticianQueryBus'

#    App\Application\Query\:
#        resource: '../src/Application/Query/**'
#        exclude: '../src/Application/Query/*Query/php'
#        tags:
#            - { name: tactician.handler, typehints: true }

#    App\Application\Command\:
#        resource: '../src/Application/Command/**'
#        exclude: '../src/Application/Command/*Command/php'
#        tags:
#            - { name: tactician.handler, typehints: true }

#    App\Ui\Http\Controller\Home\Controller:
#        class: App\Ui\Http\Controller\Home\Controller
#        arguments:
#        tags: ['controller.service.arguments']
#        calls:
#            - ['setContainer', ['@service_container', ContainerInterface]]